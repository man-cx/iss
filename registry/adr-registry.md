# ADR registry (canonical index)

**Single source of truth** for Architecture Decision Records in this repo.  
Do not maintain a second ADR list elsewhere.

| ID / date prefix | Title | ADR file |
| --- | --- | --- |
| _(add rows as ADRs are created)_ | | |

## How to add an entry

1. Add a new ADR file under `adr/` using the naming rule in `.cursor/rules/project-doc-rules.mdc`.
2. Append one row to the table above with title and link path (e.g. `adr/20260417-120000-ARC-example-decision.md`).
